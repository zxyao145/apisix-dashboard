import { MigrationPlaceholder } from "@/components/layout/migration-placeholder";

const UpstreamListPage = () => (
  <MigrationPlaceholder title="Upstreams" description="Upstream management UI is being migrated." />
);

export default UpstreamListPage;
