import { MigrationPlaceholder } from "@/components/layout/migration-placeholder";

const SSLListPage = () => (
  <MigrationPlaceholder title="SSL" description="SSL certificate management UI is being migrated." />
);

export default SSLListPage;
