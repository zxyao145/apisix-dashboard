import { MigrationPlaceholder } from "@/components/layout/migration-placeholder";

const ServerInfoPage = () => (
  <MigrationPlaceholder title="Server Info" description="Server information UI is being migrated." />
);

export default ServerInfoPage;
